class Solution {
    public int maxSubarraySum(int[] arr, int k) {
        // Code here
        int maxsum=0;
        
        for(int i =0;i<=arr.length-k;i++){
            int sum =0;
            for(int j =i;j<i+k;j++){
                sum+=arr[j];
            }
            maxsum=Math.max(maxsum,sum);
        }
        return maxsum;
    }
}
